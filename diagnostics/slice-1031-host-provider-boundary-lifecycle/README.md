# Host Provider Boundary and Lifecycle

This architecture fixture pins the coarse Alef host surface and the lifecycle
rules around generated trait bridges. It validates negotiation, framing,
registry generations, runtime-affine dispatch/finalization, snapshot leases,
cancellation, error fidelity, and shutdown without selecting a final codec.
