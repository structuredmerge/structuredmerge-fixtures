# Ruby Known-Defect Expectations

This fixture records deterministic Ruby defects as `pending` expected failures,
separate from unsupported coverage and desired golden-master behavior. The
initial entry captures the v7.1.7 `ast-template` README/API discrepancy.
