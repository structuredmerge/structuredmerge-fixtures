# Uniform Merge Provider Contract

This fixture inventories every active Ruby merge implementation that must expose
the portable provider API from slice 1018.

The matrix is intentionally complete even before implementations conform. It is
the migration work list and prevents `smorg-rb` or `ast-merge-git` from
advertising a family merely because a family-specific two-way method exists.
