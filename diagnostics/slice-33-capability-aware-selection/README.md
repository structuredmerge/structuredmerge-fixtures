# Slice 33: Capability-Aware Conformance Selection

This fixture defines the normalized selection shape for capability-aware
conformance execution. It keeps selection explicit so unsupported cases become
observable `skipped` decisions instead of silently disappearing from runner
output.
