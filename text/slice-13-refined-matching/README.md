# Slice 13 Text Content-Refined Matching Fixtures

This slice defines a two-phase text matcher: exact normalized block matching
first, then one greedy content-refinement pass over the unmatched blocks.
