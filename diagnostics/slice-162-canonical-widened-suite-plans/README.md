# Slice 162 Canonical Widened Suite Plans

This fixture records the widened canonical suite plan set only.

It still excludes the Markdown delegated nested suite. Ruby delegated nested
coverage now participates in the widened canonical suite set.
