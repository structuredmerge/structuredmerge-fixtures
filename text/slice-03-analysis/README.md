# Slice 03 Text Analysis Fixtures

This slice defines the first real text-analysis behavior:

- normalize line endings to `\n`
- trim document edges
- split blocks on blank lines
- collapse internal whitespace inside each block to single spaces
