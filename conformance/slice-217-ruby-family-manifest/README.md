# Slice 217 Ruby Family Manifest

This manifest exposes the first Ruby family portable surface without promoting
all Ruby family behavior into the canonical suite sets.

The canonical manifests may include `ruby_portable`, but the newer delegated
nested Ruby suite remains family-scoped until a later slice promotes its
review/default/replay policy explicitly.
