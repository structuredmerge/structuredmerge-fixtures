# Slice 217 Ruby Family Manifest

This manifest exposes the first Ruby portable suite descriptor without
promoting all Ruby family behavior into the canonical suite sets.

The canonical manifests may include the base Ruby portable descriptor, but the
newer delegated nested Ruby suite remains family-scoped until a later slice
promotes its review/default/replay policy explicitly.
