Shared fixture for the Kaitai-backed tree-haver substrate profile.

The analysis envelope also exercises slice 726 by carrying the schema id, source
byte length, and schema-path diagnostics returned by a runtime-backed parser.
