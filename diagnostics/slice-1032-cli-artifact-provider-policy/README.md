# CLI Artifact and Provider Availability Policy

This architecture fixture separates build-time artifact capability from
observed runtime provider availability. It pins standalone, embedded-host, and
explicit-sidecar profiles; deterministic TreeHaver and workflow-provider
selection; provider introspection; grammar acquisition policy; and fail-closed
diagnostics.
