Shared fixtures for the Rust merge-family widening slices.

`slice-1020-rust-merge3` promotes the `ruby.rust` source-preserving workflow
provider with base-aware composite, localized-conflict, and conservative
compound-use cases.
