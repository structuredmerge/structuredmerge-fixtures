# Slice 197 Markdown Family Manifest

This manifest keeps the Markdown family separate from the canonical manifest
while the substrate and parser-provider shape are still widening.

That separation also applies to the newer nested delegated Markdown suite. The
shared delegated-child transport is portable, but the nested Markdown suite is
still family-scoped and is not part of the canonical stable or widened suite
sets.
