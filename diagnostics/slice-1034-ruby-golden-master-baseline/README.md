# Ruby Golden-Master Baseline

This architecture fixture pins the clean StructuredMerge Ruby v7.1.7 source
revision, its 32-file dependency lock set, the callable behavior entry points
used by migration work, and released documentation drift that is not accepted
as golden-master behavior.
